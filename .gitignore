# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node (only used for the icon generator)
node_modules/

# Build artifact — rebuild with scripts/package.py
cpgitsync.zip

# Never commit secrets — tokens live in chrome.storage, not files
*.token
secrets.json
.env
